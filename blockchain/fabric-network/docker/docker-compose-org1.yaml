version: '3.7'

networks:
  procurement-net:
    name: procurement-net

volumes:
  peer0.procurement.example.com:

services:
  peer0.procurement.example.com:
    image: hyperledger/fabric-peer:2.5
    container_name: peer0.procurement.example.com
    environment:
      - CORE_VM_ENDPOINT=unix:///host/var/run/docker.sock
      - CORE_VM_DOCKER_HOSTCONFIG_NETWORKMODE=procurement-net
      - FABRIC_LOGGING_SPEC=INFO
      - CORE_PEER_TLS_ENABLED=true
      - CORE_PEER_PROFILE_ENABLED=false
      - CORE_PEER_TLS_CERT_FILE=/etc/hyperledger/fabric/tls/server.crt
      - CORE_PEER_TLS_KEY_FILE=/etc/hyperledger/fabric/tls/server.key
      - CORE_PEER_TLS_ROOTCERT_FILE=/etc/hyperledger/fabric/tls/ca.crt
      - CORE_PEER_ID=peer0.procurement.example.com
      - CORE_PEER_ADDRESS=peer0.procurement.example.com:7051
      - CORE_PEER_LISTENADDRESS=0.0.0.0:7051
      - CORE_PEER_CHAINCODEADDRESS=peer0.procurement.example.com:7052
      - CORE_PEER_CHAINCODELISTENADDRESS=0.0.0.0:7052
      - CORE_PEER_GOSSIP_BOOTSTRAP=peer0.procurement.example.com:7051
      - CORE_PEER_GOSSIP_EXTERNALENDPOINT=peer0.procurement.example.com:7051
      - CORE_PEER_LOCALMSPID=ProcurementMSP
      - CORE_PEER_MSPCONFIGPATH=/etc/hyperledger/fabric/msp
      - CORE_OPERATIONS_LISTENADDRESS=0.0.0.0:9443
    working_dir: /opt/gopath/src/github.com/hyperledger/fabric/peer
    command: peer node start
    volumes:
      - /var/run/:/host/var/run/
      - ../crypto-material/peerOrganizations/procurement.example.com/peers/peer0.procurement.example.com/msp:/etc/hyperledger/fabric/msp
      - ../crypto-material/peerOrganizations/procurement.example.com/peers/peer0.procurement.example.com/tls:/etc/hyperledger/fabric/tls
      - peer0.procurement.example.com:/var/hyperledger/production
    ports:
      - 7051:7051
      - 9443:9443
    networks:
      - procurement-net
